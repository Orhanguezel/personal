// Stub service worker to prevent Next.js locale route from handling
// /firebase-messaging-sw.js requests in dev.
// If you later enable Firebase Messaging, replace this file with
// the official Firebase service worker implementation.

self.addEventListener('message', () => {});
